# Runbook — 定时任务注册表（本地 macOS + 62 + 77）

> **前置**：本文命令块里的 `$NVY_*` 从仓外解析 —— 本机跑先 `. ~/.nvy/fleet.env`，主机上跑先 `. /etc/nvy-fleet.env`。变量清单与角色说明见 [`ops/host/fleet.env.example`](../host/fleet.env.example)；为什么真值不入库见 [`information-boundary.md`](../../docs/conventions/information-boundary.md)。
>
> 三处 host/OS 级定时任务的**单一真相源（SoT）**：开发机（macOS launchd）、生产 62（`mbw-indexer`，systemd）、生产 77（`mbw-staging`，systemd）。
> 「哪台机器在跑什么定时任务、几点跑、跑什么脚本、仓库里对应哪个文件」一处查清。
>
> **维护纪律**：新增 / 改 / 退役任何定时任务 → 同 PR 更新本表（path rule `.claude/rules/scheduled-tasks-registry.md` 在触及 `.timer` / 本地生成器时自动提醒）。
>
> **不在范围**（见末节）：server 内部 `@nestjs/schedule @Cron` · OS 自带 systemd timer。
>
> 涉及主机 62 / 77 的定位（账号 / IP / 规格 / 角色）见 [`host-inventory.md`](./host-inventory.md)。

## 本地（macOS，launchd，`~/Library/LaunchAgents/`）

| 任务               | 类型 / 调度                                     | launchd label                 | 实际执行                                                                                                                                                                                                                                             | 仓库锚点（生成器）                          | 用途                                                                                                                                                |
| ------------------ | ----------------------------------------------- | ----------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------- |
| holdings 同步      | 定时 · 每日 **09:00**                           | `com.nvy.holdings-sync`       | `~/.nvy/holdings-sync/run-scheduled.sh` → `~/.nvy/holdings-sync/node_modules/.bin/tsx ~/.nvy/holdings-sync/sync.ts --headless --base-url https://api.shintongtech.com`（**自包含副本**，脱离 git worktree）                                          | `scripts/jobs/holdings-sync/setup.ts`       | 持仓同步到 prod（77 的 api 域名）                                                                                                                   |
| marketdata 同步    | 定时 · 每日 **09:05**                           | `com.nvy.marketdata-dev-sync` | `~/.nvy/marketdata-dev-sync/run-scheduled.sh` → `~/.nvy/marketdata-dev-sync/sync.sh`                                                                                                                                                                 | `scripts/jobs/marketdata-dev-sync/setup.sh` | 行情数据同步（targets 77）                                                                                                                          |
| cc-connect 遥控    | **常驻 daemon**（非定时；RunAtLoad+KeepAlive）  | `com.cc-connect.mono`         | `~/.cc-connect/bin/cc-connect --config ~/.cc-connect/config.toml`                                                                                                                                                                                    | （仓库外，`~/.cc-connect/`）                | 飞书遥控本地 Claude Code                                                                                                                            |
| no-show 看门狗     | 定时 · 每日 **10:00**                           | `com.nvy.watchdog`            | `~/.nvy/watchdog/run-scheduled.sh` → `~/.nvy/lib/nvy-watchdog.sh "holdings-sync:90000 marketdata-dev-sync:90000 nvy-private-backup:90000"`                                                                                                           | `scripts/jobs/nvy-watchdog/setup.sh`        | 查本地每日任务心跳，no-show 推飞书                                                                                                                  |
| 私有文档异地备份   | 定时 · 每日 **09:30**                           | `com.nvy.nvy-private-backup`  | `~/.nvy/nvy-private-backup/run-scheduled.sh` → wrapper `nvy-private-backup` → `~/.nvy/nvy-private-backup/backup.sh`：`git bundle --all` → `age` 公钥加密 → `ssh` 落代号 `app` 的 `~/nvy-private-backup/`（远端保留 30 份，回读字节数核对防静默截断） | `scripts/jobs/nvy-private-backup/setup.sh`  | 仓外私有文档仓 `~/nvy-private`（`docs/private` + `experience` + `explore` 的真身）的异地密文副本                                                    |
| quant-win 健康探针 | **高频监控** · 每 **30 min**（`StartInterval`） | `com.nvy.quantwin-health`     | `~/.nvy/quantwin-health/run-scheduled.sh` → `~/.nvy/quantwin-health/probe.sh`：`aliyun ecs RunCommand` 打一段 PowerShell 回读「C: 可用 MB + 交易进程存活 + 外部已建连数 + uptime」，越阈值 / 进程缺失 → `feishu_send` **合并一条**                   | `scripts/jobs/quantwin-health/setup.sh`     | 代号 `quant-win`（实盘终端宿主）**无 CloudMonitor agent 也无 SSH** ⇒ 云监控采不到磁盘指标、配不出告警；本探针从机外补这个面，不在实盘机上装任何东西 |

> 顺序约定：holdings（09:00）须先于 marketdata（09:05）——**非 FK 硬依赖**（`portfolio.holding` 对 `marketdata.instrument` 无外键，market+code 字符串引用；holdings 走 prod API 服务端算好再写），对调安全，rationale 详见下节 [故障排查](#故障排查本地每日同步)。两个 `run-scheduled.sh` 由仓库 `scripts/*/setup.*` 生成（脚本头注明「请勿手改，重跑 setup 覆盖」），改调度/目标改生成器后重跑 setup。
>
> **自包含副本（两者皆是）**：两个任务都跑 `~/.nvy` 下的**冻结副本**、**不依赖任何 git worktree**——marketdata 由 setup `cp sync.sh`；holdings 由 `setup.ts:materializeRuntime` 把运行时源码拷入 + 在 `~/.nvy/holdings-sync` 装独立 `node_modules`（`playwright-core` + `tsx`，约 23M，`pnpm install --ignore-workspace`）。**根治** worktree 被删/切分支即停摆（见 06-22 holdings 因 worktree 删停摆教训）。代价：改 sync 代码须重跑对应 setup 覆盖副本。
>
> **跑完上报（每跑必报）**：两个本地每日同步都**套通用 wrapper** `ops/lib/nvy-run-reported.sh`（setup 拷一份到 `~/.nvy/lib`），跑完**成功 / 失败都推**一条飞书 report（机器 + 任务 + 耗时 + stdout 末尾数字：holdings 持仓 / 已清仓 / 交易数，marketdata 逐表计数）+ 写心跳 `~/.nvy/heartbeats/<task>.beat`。飞书传输集中在 `ops/lib/feishu-send.sh`（openssl 签名免 python3），全机复用，sync 脚本**零飞书代码**（只管干活 + 打 stdout + exit 0/非0）。配置走共享文件 `~/.nvy/feishu-alert.env`（`NVY_ALERT_WEBHOOK_URL` + `NVY_ALERT_FEISHU_SECRET`；签名校验启用后 secret 必填；仓库外、含 secret 不入库），launchd wrapper `set -a` source 它；**webhook 缺失 → 静默跳过**（优雅降级）。
>
> **no-show 看门狗**：wrapper 在任务进程内推，看不见任务**根本没跑**（Mac 睡死 / plist 丢 / launchd 没醒）→ 既无成功 report 也无失败告警 = 静默。看门狗 `com.nvy.watchdog`（每日 10:00，`ops/lib/nvy-watchdog.sh`）查上述心跳过去 ~25h 有没有写过，没写就推飞书。**局限**：本机看门狗逃不出「整机宕 / 全程睡死」——那种唯有**外部**监控能兜（healthchecks 之类；单人 3 机暂不上，见 [子plan 调研结论](../../docs/private/plans/2026-06/06-26-scheduled-tasks-report-wrapper-watchdog.md)）。
>
> **`quantwin-health` 为何既不套 wrapper 也不进看门狗清单**：它是**高频监控**（每 30 min），per `.claude/rules/scheduled-tasks-registry.md` §①.3 —— 套 `nvy-run-reported.sh` 会每半小时推一条 report 刷屏，故保留自身告警逻辑（飞书发送仍只走 `feishu_send`，满足 CI 硬门 A）。它的正向 liveness 走**每日摘要**：健康时每天首次跑过 `NVY_QUANTWIN_SUMMARY_HOUR`（默认 9 点）推一条 🟢，**这条本身就是「探针还活着」的证据** —— 摘要该来没来 = 探针死了，等价于看门狗对每日任务的作用。⚠️ 它自身的探测失败（云助手不可达 / 超时）**连续 2 次**才告警，单次抖动不报（云 API 偶发超时常见）。⚠️ 阈值定标依据写在 `quantwin-health.env.example` 里，别凭感觉改。
>
> **心跳目录解析**：wrapper（成功 / 失败都）写 `<dir>/<task>.beat`，看门狗读同一 `<dir>`，故二者必须落同处。`<dir>` = `NVY_HEARTBEAT_DIR`（若设）否则 `${HOME:-/var/lib/nvy}/.nvy/heartbeats`。本地 launchd 有 `$HOME` → `~/.nvy/heartbeats`；**prod systemd 不注入 `$HOME`**（裸 `$HOME` 在 `set -u` 下会 unbound 崩溃，故 wrapper 用 `${HOME:-/var/lib/nvy}` 兜底），套 wrapper 的 systemd 任务（如 77 cert）在 `/etc/nvy-alert.env` 显式设 `NVY_HEARTBEAT_DIR=/var/lib/nvy/heartbeats`（干净路径；同文件被 cert + 看门狗两个 unit 共享 source，自然一致）。
>
> **已退役 —— `com.nvy.futu-eod-poc`（futu 期权链 EOD 快照 POC，2026-07-30 装 → 2026-08-13 摘）**：本机拉起 OpenD 抓 7 只 US 标的落 SQLite（sellput-viz p3 一周试跑）。**摘的判据是数据面已被港机链路整体取代**，不是它坏了：`marketdata.option_daily_snapshot`（经 `broker-hk` 的 futu-shim `/chain` `/snapshot` 采）覆盖 12 只锚定标的、**含 POC 那 7 只全部**，字段是 POC `chain_snapshot` 的**严格超集**，且带 POC 没有的三道守门（FR-045 逐合约覆盖率 + FR-046 两级补救 + 08:00 完整性探针）；原定留存条件「等 M2b impl 完成后一并评估」随 M2b 于 08-07 ship 而满足。**历史样本 `~/futu-screener/eod_snapshots/eod.sqlite`（07-30~08-12，16 个 session）刻意保留** —— 它是与港机链路做数值对照的唯一独立数据面，`eod_snapshot.py` / `cboe_eod_snapshot.py` 同样留作按需人工跑的离线工具。
>
> ⚠️ **退役当天暴露一个对留下来的任务同样成立的洞：wrapper 抓不到「子进程挂死」**。`nvy-run-reported.sh` 是**进程内**上报且**无 timeout** —— 08-13 那次 OpenD 登录失败后 futu SDK 无限重连、进程 11.5h 不退，于是**成功 report 与失败告警都不发**；而看门狗只查心跳**年龄**，25h 窗内看不出「跑了但卡住」。⇒ 挂死是本地这套感知面的**共同盲区**（holdings / marketdata 同理），目前唯一兜底是次日看门狗的 no-show。

## host `index`（`root@$NVY_INDEX_HOST`，systemd · 接地检索专用 host）

| 任务             | 调度           | timer unit                           | ExecStart                          | 仓库锚点                                                                                                                           | 用途                                                                      |
| ---------------- | -------------- | ------------------------------------ | ---------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------- |
| 接地检索增量     | 每 **~2min**   | `code-index-tick.timer`              | `scripts/cron-tick.sh`             | `services/code-index/deploy/code-index-tick.{service,timer}` + `services/code-index/scripts/cron-tick.sh`                          | git-diff-by-SHA 增量 re-embed（ADR-0060）                                 |
| 接地检索陈旧告警 | 每 **5min**    | `code-index-freshness-monitor.timer` | `scripts/check-index-freshness.sh` | `services/code-index/deploy/code-index-freshness-monitor.{service,timer}` + `services/code-index/scripts/check-index-freshness.sh` | drift > 30min → 飞书告警（保留自身 grace；仅失败推，走 `feishu-send.sh`） |
| 接地检索每日摘要 | 每日 **09:10** | `code-index-daily-digest.timer`      | `scripts/daily-digest.sh`          | `services/code-index/deploy/code-index-daily-digest.{service,timer}` + `services/code-index/scripts/daily-digest.sh`               | 每日推正向 liveness（24h 索引提交数 + 追平状态 + tick 活性）              |

> 完整部署 / 运维 SoT：[`code-index-deploy.md`](./code-index-deploy.md)。`cert-expiry-monitor.timer` 在 62 上存在但 **disabled**（迁移残留，真身在 77，勿误以为在跑）。

## host `app`（`$NVY_APP_HOST`，systemd · 业务 server host）

| 任务                | 调度                             | timer unit                              | ExecStart                                                                                                                                                                                                                         | 仓库锚点                                                                                                             | 用途                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ------------------- | -------------------------------- | --------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| TLS 证书自动续期    | certbot 默认（每日两次，随机化） | `certbot.timer`                         | `certbot renew`（+ deploy-hook）                                                                                                                                                                                                  | 系统 certbot + `ops/host/cert/hooks/`（`00-copy-volume-reload.sh` / `10-oss-putcname-img.sh`）                       | LE 证书续期 → nginx / OSS                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| TLS 证书过期告警    | 每日 **08:30**                   | `cert-expiry-monitor.timer`             | `nvy-run-reported cert-expiry -- jobs/cert-expiry-monitor.sh`                                                                                                                                                                     | `ops/jobs/cert-expiry-monitor.sh` + `ops/jobs/systemd/cert-expiry-monitor.{service,timer}`                           | 套 wrapper：每日推 report（逐域剩余天数），< 21d / 握手失败 → 🔴                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| marketdata 同步报告 | 每日 **09:00**                   | `marketdata-sync-report.timer`          | `nvy-run-reported marketdata-sync-report -- jobs/marketdata-sync-report.sh`                                                                                                                                                       | `ops/jobs/marketdata-sync-report.sh` + `ops/jobs/systemd/marketdata-sync-report.{service,timer}`                     | 套 wrapper：每日只读查 prod `sync_run` 推逐维度成败 + 计数；任一维度 failed/partial/interrupted（#137 部署打断且其后无重跑接上）/卡 running、或交易日零行（停摆）、或**零行且日历不健康**（044 FR-012）→ 🔴。另含一道**日频数据完整性闸**（局部塌陷：某交易日行数不足前后两邻日的一半 → 🔴）—— 上面那些判据全是 run-centric 的，**窗口一滑过去就看不见失败留下的数据窟窿**；2026-08-07 熔断级联后 daily_bar 少 96%、short_selling/volatility 整天为 0，而报告连报 4 天「20 维度 20✅」，本闸即为补这只眼睛                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 交易日历健康探针    | 每 **4h**（00/04/…/20）          | `marketdata-calendar-health.timer`      | `nvy-run-reported marketdata-calendar-health --on-success silent -- jobs/marketdata-calendar-health.sh`                                                                                                                           | `ops/jobs/marketdata-calendar-health.{sh,sql}` + `ops/jobs/systemd/marketdata-calendar-health.{service,timer}`       | 套 wrapper（**仅失败推**）：**心跳档**只读探 `marketdata.calendar_sync_health`，cn/hk/us 任一心跳 > 26h 陈旧、或 `served_by` 非**该市场**主源（cn/hk=`tencent`、us=`futu`）→ 🔴（044 FR-010 / FR-014；us 于 2026-07-31 随换源纳入监控面）；**视野档**（062 FR-016）只读探 `marketdata.calendar_coverage`，缺行 / `covered_to` 早于今天 / 余量不足 5 个交易日且未抵**当年** 12-31 → 🔴（年末豁免只在当年成立，跨年后转红是设计，别「修」）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 静态日历年更提醒    | 每月 **1/15 号 09:15**           | `static-calendar-annual-reminder.timer` | `nvy-run-reported static-calendar-annual-reminder --on-success silent -- jobs/static-calendar-annual-reminder.sh`                                                                                                                 | `ops/jobs/static-calendar-annual-reminder.sh` + `ops/jobs/systemd/static-calendar-annual-reminder.{service,timer}`   | 套 wrapper（**仅催更期推**）：只读比对 77 checkout 里 `static-calendar.data.ts` 的 `STATIC_CALENDAR_COVERAGE.to` vs 当前年月，未覆盖次年且入 Q4（10 月起）→ 📅/🔴 催更，覆盖到次年后自动静默。**不入看门狗**（月级稀疏调度；兜底 = calendar-health 探针）。见下「年度人工任务」段                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 表级数据健康探针    | 每 **4h**（02/06/…/22）          | `marketdata-table-health.timer`         | `nvy-run-reported marketdata-table-health --on-success silent -- jobs/marketdata-table-health.sh`                                                                                                                                 | `ops/jobs/marketdata-table-health.{sh,sql}` + `ops/jobs/systemd/marketdata-table-health.{service,timer}`             | 套 wrapper（**仅失败推**）：只读探各维度**事实表里的数据年龄**（哨兵票 + 按交易日折龄）。判红条件：某 (维度, 市场) 全部哨兵落后 > 阈值 · `us_equity_bar` 在 `need_sync` 白名单里**任一只**掉队（OR）· `underlying_iv_daily` 在同一白名单里**全部**陈旧（AND —— 批量快照端点 + 无期权标的整行缺席是正常态，取 OR 会永久假红）· us 工作集为空 · `us_index_daily` 的 VIX/VVIX **任一个**缺数（两个固定代码，**不挂锚闸**）· 日历算不出基准 · **跨市场幽灵合约**（同一合约标识同时挂在 us 与 hk 标的名下，#199）· **OI 归属标签漂移**（按合约**真实市场**重算的 `oi_as_of` 与库里不符，#262；市场通用，窗口 = 最近 3 个交易日，判据是 `oiRefreshedAtEod` 的第三处表达 —— 改 `MARKET_OI_SETTLE_LOCAL_MINUTE` 必须同步改它）→ 🔴。**与 calendar-health 互补，别混**：那个探「日历填充心跳」（liveness），这个探「数据还在不在长」                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 期权快照完整性探针  | 每日 **08:00**                   | `marketdata-snapshot-integrity.timer`   | `nvy-run-reported marketdata-snapshot-integrity --on-success silent -- jobs/marketdata-snapshot-integrity.sh`                                                                                                                     | `ops/jobs/marketdata-snapshot-integrity.{sh,sql}` + `ops/jobs/systemd/marketdata-snapshot-integrity.{service,timer}` | 套 wrapper（**仅失败推**）：只读核对期权快照的**逐合约覆盖率**。分母 = 基线日快照里、到期日 **≥** 当日的合约集（按 contract_id 去重；`>=` 不是 `>`，当日到期的腿当日仍可采）；分子 = 当日实得。**逐票**判定 `covered < expected × 阈值`（起手 100%），某票跌破 → 🔴。分母为空判「无对象」不告警。🚨 **它是 FR-046「完整性 ERROR 的触达 MUST NOT 并入次日日报」的唯一载体** —— 与 09:00 的 marketdata-sync-report **完全解耦**，别合并。**与 table-health 互补**：那个抓「维度级停摆」，这个抓「维度在长但某票少了 48 张合约」。调度时点是判据的一部分（08:00 > 06:30 采集窗、< 09:00 日报、且北京 08:00 仍在 ET 同一交易日内），**别随手改**。**服务 us + hk 两个市场**（#267，单文件市场通用；**无第二条 timer** —— 北京 08:00 对港股同样成立，hk 的「当日」= HKT 当日减一天，那一场在 HKT 00:33–00:36 已采完）。摘要两段、以双竖线分隔、每段分别以 `US` / `HK` 开头；`exit_code` = 两市场取 max（任一不健康即红）。**非交易日谓词自己短路（三档）**：① 周末（纯`isodow`，不读表）② 三态 `unknown`（日历没填到该日）⇒ **fail-closed 判红**，不静默放行 ③ 三态 `non-trading`（无行且落在 `calendar_coverage`已声明区间内）⇒ exit 0「非交易日不判」。不推飞书但**心跳照写** ⇒ timer 保持每日跑、看门狗 25h 阈值不动（收成`Tue..Sat`会开出 72h 空档，把假红换成假 no-show）。✅ **公众假期假红已修**（#276，此前 ~9-10 次/年）—— 原「查`trading_day` 就是循环信任」那个二选一是假的：被禁的是「查日历 → 静默放行」，而三态在放行前要求日历**正向声明**覆盖了今天，陈旧则判红 |
| 应用状态健康探针    | 每 **4h**（03/07/…/23）          | `app-state-health.timer`                | `nvy-run-reported app-state-health --on-success silent -- jobs/app-state-health.sh`                                                                                                                                               | `ops/jobs/app-state-health.{sh,sql}` + `ops/jobs/systemd/app-state-health.{service,timer}`                           | 套 wrapper（**仅失败推**）：只读探**非 marketdata 侧**的应用状态 —— #209 实证③ 发现三个既有探针的谓词全部只碰 `marketdata` schema，`optionsdesk` / `public.outbox_event` 零进程外监控。判红条件：active 锚的 `last_close_date` **全部**落后 > 2 交易日（AND 语义 —— 单只停牌/退市不判红，代价是单只掉队看不见，summary 报 `fresh/total` 让它可见）· active 锚为 0（空工作集）· **任一**市场算不出 expected_day（逐市场 fail-closed）· `outbox_event` 有 `published_at IS NULL` 且超 15 分钟的事件（relay 每 10s 跑一次 ⇒ 90 倍余量）。**它补的是 `sync-anchor-quote.scheduler` 那条真空**：失败路径是 `catch → logger.error → return null`，**不落 `sync_run`**，此前没有任何东西看得见                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| 港机 shim 存活探针  | 每 **4h**（01/05/…/21）          | `futu-shim-health.timer`                | `nvy-run-reported futu-shim-health --on-success silent -- jobs/futu-shim-health.sh`                                                                                                                                               | `ops/jobs/futu-shim-health.sh` + `ops/jobs/systemd/futu-shim-health.{service,timer}`                                 | 套 wrapper（**仅失败推**）：经 wg1 隧道探港机 `/healthz`（只读、不启动 OpenD、不刷 idle 时钟）。判红条件：重试 3 次仍不可达（整机宕 / 隧道断 / shim 挂）· `opend_unit_active=false`（`Restart=always` 拉不回）· `opend_connected=true` 且 `qot_logined=false`（网关活着但登录态掉）→ 🔴。**探针在 77 不在港机**：隧道断 / 港机宕这两种港机自己报不出来。判据与「显式不报的三种」见 `ops/jobs/futu-shim-health.sh` 头部；排障 [`futu-opend-hk.md`](./futu-opend-hk.md)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| PG 本地备份         | 每日 **08:40**                   | `backup-pg.timer`                       | `nvy-run-reported backup-pg -- jobs/backup-pg.sh`                                                                                                                                                                                 | `ops/jobs/backup-pg.sh` + `ops/jobs/systemd/backup-pg.{service,timer}`                                               | 套 wrapper（**每日推 report**，唯一的数据保护机制值得一条正向 liveness）：`pg_dump \| gzip` → `/home/admin/backup/`，保留最新 **3 份**。产物三判据：`gzip -t` · 体积 ≥ 上一份的 50%（**相对值** —— 47 天涨 5.8×，绝对阈值必过时）· 先写点文件验过再 `mv`（防截断产物顶着正常文件名留在盘上）。🚨 **本机唯一副本，无异地**（2026-08-08 撤 OSS，#887；异地冗余后续另议）⇒ 只覆盖误 DROP / 灾难 migration，**不覆盖**盘坏 / 实例丢。**产物是 `root:0600`**（unit 以 root 跑是刻意的，理由见 unit 头）⇒ **恢复时要 `sudo` 才读得到**。🚨 **08:40 是判据的一部分**：晚于 ~06:30 期权链采集窗与 08:00 完整性探针、早于 09:00 日报 ⇒ 每份 dump 都含当日那批「漏采即永久缺口」的期权快照                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| no-show 看门狗      | 每日 **10:00**                   | `nvy-watchdog.timer`                    | `nvy-watchdog.sh cert-expiry:90000 marketdata-sync-report:90000 marketdata-calendar-health:43200 marketdata-table-health:43200 futu-shim-health:43200 marketdata-snapshot-integrity:90000 backup-pg:90000 app-state-health:43200` | `ops/jobs/systemd/nvy-watchdog.{service,timer}` + `ops/lib/nvy-watchdog.sh`                                          | 查 cert-expiry + marketdata-sync-report + calendar-health + table-health + futu-shim-health + snapshot-integrity + backup-pg 心跳，no-show 推飞书                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |

> 完整证书生命周期 SoT：[`cert-management.md`](./cert-management.md)。**所有飞书推送**（每日任务 report、freshness drift 告警、每日摘要、看门狗 no-show）**全部复用同一飞书 bot**，统一走 `ops/lib/feishu-send.sh`；公共 webhook/secret **每机一个共享文件**（本地 `~/.nvy/feishu-alert.env`、62/77 `/etc/nvy-alert.env`，同一套 `NVY_ALERT_WEBHOOK_URL` / `NVY_ALERT_FEISHU_SECRET` 变量；任务调参如 `INDEX_STALE_WINDOW_MIN` / `CERT_ALERT_THRESHOLD_DAYS` 仍各归各任务 env）。**bot 校验改用「签名校验」**（`NVY_ALERT_FEISHU_SECRET`）——放开了旧「自定义关键词=告警」约束（否则成功 report 不含「告警」会被关键词 bot 拒收）。
>
> **交易日历健康探针（044）—— 三条反直觉设计，改之前先读**：
>
> 1. **判据不在 bash 里，全在 `ops/jobs/marketdata-calendar-health.sql`**（单一共享产物）。`ops/jobs/marketdata-calendar-health.sh` 只做「跑谓词 → 打摘要 → `exit` 谓词给的码」，**无 if / 无阈值**。原因：仓内无 bash 测试框架 → bash 里的判断必然无测试覆盖（撞宪法 §II NON-NEGOTIABLE）；把判断下沉到 SQL 后，它被两个 Testcontainers IT **真测**：`marketdata.calendar-044.health.it.spec.ts`（心跳档：埋 25h/27h 心跳 + 主源/降级 `served_by`）与 `marketdata.calendar-062.horizon-probe.it.spec.ts`（视野档：埋余量 2/8 个交易日、当年末终点、跨年旧年末、声明缺失）。⇒ **改判据改 SQL，别改 bash；也别在 bash / 别处内联复制那段 SQL**（两份必漂移，一漂移「判断已被真测」就成了假话）。同一条谓词被 `marketdata-calendar-health.sh` 与 `marketdata-sync-report.sh` **共用**。
> 2. **`--on-success silent`（健康不推）—— 偏离「每日批任务成功也推」的默认，故意为之**：每 4h × 成功也推 = 6 条/日刷屏 → 训练出「反正都 ✅，不看了」，真告警来时被淹没。代价 = 没有正向 liveness → **由看门狗兜**（`marketdata-calendar-health:43200`，≈12h/3 次 no-show 才报）。这两个决定是一对，**只删其一 = 探针停摆后彻底静默**。
> 3. **探针不经 app 进程**（FR-010 的结构性要求）：直读 PG（`docker exec psql` 只读，同 sync-report 通路）。app 整个挂掉 → 心跳自然陈旧 → 照样告警。走 app 的健康端点做不到这点（app 死了谁都不响）。
>
> **表级数据健康探针（2026-08-02 新增）—— 与上者同范式，但判的是另一回事**：
>
> 1. **它补的洞**：2026-08-01 四个维度在 prod 静默丢数、`sync_run` 连续十几天全绿无人发现（`announcement` 停 12 个交易日 / `buyback` 同期零增量 / `shareholder_change` 一周只采 1/7 / `fund_holding` 上线 18 天 0 行）。既有三道防线全部漏判：`alertIfDegraded` 只看 `failed`、`FreshnessSlaCheck` 量的是 **run 年龄**而非**数据年龄**（跑了但没产出 → run 永远新鲜）、`rowsFetched` 全仓只有一处埋点。⇒ 判据必须挂在**数据**上、跑在**采集进程之外**。日历探针探的是「日历填充心跳」，覆盖不到这一类。
> 2. **判据是「逐票哨兵 EXISTS」而不是「全表 `max(date)` + 行数下界」——性能是硬约束不是偏好**：被监控表的索引一律 `(instrument_id, date)` 前导，**没有任何 date 前导索引** ⇒ `max(date)` 是全表扫（`volatility_daily` 1590 万行 / 1.9 GB），而 77 可用内存仅 ~580 MB 且无 swap，每 4h 冲一次 page cache = 探针自己变成事故源。逐票走 uk 索引，实测全谓词 **74 ms**。**想改回聚合判据 → 先加 date 前导索引**。
> 3. **AND / OR 语义是分开定的，别统一**：cn/hk 侧每个 (维度, 市场) 配 **2 只哨兵、全陈旧才判红**（个股停牌不误报，代价是单票掉队看不见）；us 侧因白名单只有个位数且每只都是 045 雷达锚定标的，取 **任一掉队即红**。⚠️ 聚合键必须是 **(维度, 市场)** 不能只按维度 —— `eod_bar`/`fundamental` 的 marketScope 是 {cn,hk}，只按维度聚合时 cn 侧整个停摆会被 hk 侧健康值**平均掉**判绿（首版就是这么写的，被 IT 变异用例当场抓出）。这条与 044「不被健康市场平均掉」是同一教训。
> 4. **覆盖边界（说清楚没覆盖什么）**：只抓「维度级停摆」，**不抓单票掉队**（严格版属期权链完整性核对，随 p4 落）。报告期语义（`fund_holding` / `revenue_segment` 等）、覆盖式 upsert（`index_membership` 等）、稀疏事件流（`buyback` / `allotment` 等）三类**不适用数据年龄判据**，硬塞进来只会制造长期噪音，故不在本批监控面内。
>
> **收到 🔴 怎么办**：摘要行直接给出每个市场的心跳龄 + `served_by`（如 `cn=27h前⚠陈旧/tencent | hk=1h前/static⚠降级 | us=1h前/futu`）。① **⚠陈旧** → 日历填充在失败，查 server 日志 `trading-calendar-sync` 的 `lastError`（`select * from marketdata.calendar_sync_health;`）；② **⚠降级** → 该市场主源失效、下一层在兜底，属需人工介入态（查主源是否改了接口/被封）。**降级 ≠ 健康**。分市场看：
>
> | 市场    | 链                       | 降级意味着                                                                                                                                                                                                               |
> | ------- | ------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
> | cn / hk | `[腾讯 L1, 静态年历 L2]` | 腾讯挂了、静态层在兜 —— **L2 仅覆盖当年**，不修则跨年时静态表耗尽全盘爆炸                                                                                                                                                |
> | us      | `[富途 L1, 腾讯 L2]`     | 富途 shim / OpenD / B↔C 隧道有一环断了（先 `curl http://10.89.0.1:8811/healthz`，见 [`futu-opend-hk.md`](./futu-opend-hk.md)）。**us 蓄意无 L3** → 腾讯再挂就没有兜底了，此时靠人工 `marketdata-trading-day-seed` CLI 补 |
>
> **us 有意不在监控面内**：L2 静态层不覆盖 us → us 陈旧既无备源可用、也无从修，纳入只会制造修不掉的常亮告警（狼来了 > 漏报）。
>
> **手动跑任何谓词时 `\i` 一定失败（五个 `.sql` 通用）**：`psql` 跑在**容器里**，而谓词在**宿主**的 `/usr/local/lib/nvy/jobs/` 下 ⇒ `\i /usr/local/lib/nvy/jobs/<x>.sql` 报 `No such file or directory`（那个路径在容器内不存在，不是文件没装）。**所有探针脚本用的都是 `docker exec -i … psql … -f - < "$PREDICATE_SQL"`** —— 从宿主把文件**内容**管进 stdin；手动排查时照抄这个形状，别自己想当然用 `\i`。2026-08-27 实撞（当时在验 #209 的读侧渲染）。
>
> ### 已知数据空洞（登记制 —— 看到序列断档先查这里，别重新排查一遍）
>
> 期权快照**漏采即永久缺口**（vendor 不提供历史交易日的期权快照，买不回来）。已确认、已归因、
> **不会再自愈**的空洞登记于此；探针对它们照常判红直到窗口滑过，那是预期行为。
>
> | 标的     | 缺失交易日                     | 成因                                                                                                                                                                                                                           | 关联                                                                                                                                          |
> | -------- | ------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------- |
> | `us:ALB` | **2026-08-24 / 08-25 / 08-26** | vendor 按词根串市场（形态 B）在 08-22 把 146 行幽灵合约写进该票名下 ⇒ 每晚快照第一批必 502「未知股票」、整票零采。根因于 08-27 15:59 随 migration 修复；其中 08-26 当天本可由 ② 级盘前兜底补回，因**基线日是全局的**而未被识别 | [#199](https://github.com/zhangleizlpd/no-vain-years-tech/issues/199) · [#231](https://github.com/zhangleizlpd/no-vain-years-tech/issues/231) |
>
> **登记纪律**：只登记「已确认成因 + 不可回补」的。可回补的走 ② 级兜底，不要写进来 —— 这张表
> 一旦混进「其实还能补」的行，下一个人就会拿它当借口不去补。
>
> **`marketdata-sync-report.sh` 的循环信任盲区（044 FR-012）—— 改它的零行分支前必读**：该看守在「窗口零行」时会查 `trading_day` 判「昨日是否交易日」区分真停摆 vs 正常非交易日。**病根**：它拿**那张可能已坏的表**当判据 —— 2026-07 事故里日历填充静默失败 → `trading_day` 停更 → 夜间 tick 认为「非交易日」不组 flow → 零行 → 看守读同一张坏表 → 判「非交易日，无同步属预期」→ ⏭️ 放行 → **每天绿灯，潜伏 2 天**。修法 = 在「放行」**之前**先跑健康谓词问「这张表还可信吗」（判据来自表之外的心跳）。⇒ **档位顺序是承重的**，健康档挪到「昨日非交易日 → 放行」之后 = 盲区原样复活。
>
> **人工验证步骤（bash 侧无单测 —— 这是诚实兜底，改 `marketdata-sync-report.sh` 零行分支后请照跑）**：对 **dev DB**（`mbw-poc-postgres`，**绝不对 prod 跑**）埋数据实跑，四态：
>
> ```bash
> docker compose -f docker-compose.dev.yml up -d --wait
> export SYNC_REPORT_PG_CONTAINER=mbw-poc-postgres SYNC_REPORT_PG_USER=mbw SYNC_REPORT_PG_DB=mbw_poc
> # 前置：确保 marketdata.sync_run 窗口(18h)内零行 → 才会进零行分支
> # 埋 trading_day 近 30 日已 populate（recent_pop>0），昨日在/不在其中 = 控制 y_trading
> # 埋 marketdata.calendar_sync_health 的 last_success_at / served_by = 控制健康
> ./ops/jobs/marketdata-sync-report.sh; echo "exit=$?"
> ```
>
> | 埋的状态（零 sync_run 行）                               | 期望                                                                                |
> | -------------------------------------------------------- | ----------------------------------------------------------------------------------- |
> | 日历不健康（心跳 30h 陈旧）+ 昨日非交易日                | `exit 1` 🔴「日历不健康，无法判定停摆」← **本次修复的靶子；旧版此态 `exit 0` 放行** |
> | 日历降级（`served_by='static'`，心跳新鲜）+ 昨日非交易日 | `exit 1` 🔴（降级 ≠ 健康，FR-014）                                                  |
> | 日历健康 + **昨日为交易日**                              | `exit 1` 🔴 疑似停摆（既有能力不回归，FR-013）                                      |
> | 日历健康 + 昨日非交易日                                  | `exit 0` ⏭️ 放行（既有能力不回归，FR-013）                                          |
>
> 另：谓词文件缺失 / psql 失败 → **保守告警 `exit 1`**，禁止静默放行。跑完记得清掉埋的伪造数据（`TRUNCATE marketdata.trading_day; TRUNCATE marketdata.calendar_sync_health;` —— 两表在 dev sync 里都是 `skip`，**不会**被每日 09:05 同步复原）。

## 年度人工任务（人工执行 · 覆盖感知提醒兜底）

| 任务                 | 时点                                                      | Owner           | 动作                                                                                                                                                                                                                                         |
| -------------------- | --------------------------------------------------------- | --------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **静态交易日历年更** | HKEX 发布**次年**年历后、**当年 12 月前**跑完（每年一次） | `@zhangleizlpd` | `brew install poppler` → 取 HKEX 次年 Stock Connect 年历 PDF → `pdftotext -layout` → `pnpm tsx scripts/checks/gen-static-calendar.ts --year <次年> --in calendar.txt` → 产出 `apps/server/src/marketdata/static-calendar.data.ts` 入库（PR） |

> **执行仍是人工**（官方只发 PDF、每年格式微调，不能全自动生成——见下），但**「会不会忘」已被消灭**：`static-calendar-annual-reminder.timer`（77，每月 1/15 号）读 committed `static-calendar.data.ts` 的覆盖终止年，未覆盖次年且入 Q4（10 月起）→ 飞书催更（10/11 月 📅 → 12 月 🔴 → 跨年未更 🔴🔴）；**年更 PR 一部署到 77，覆盖年份跳到次年 → 下次跑即静默、自动停催**。故本任务从「纯靠人记得」降级为「人工执行 + 定时催更兜底」（044 analyze A6 + 2026-07 追加提醒器）。判据是纯日期算术（standalone probe，照 cert-expiry 先例），本地多态验证：`NVY_STATIC_CALENDAR_FILE=<fixture> NVY_FAKE_TODAY=YYYY-MM-DD bash ops/jobs/static-calendar-annual-reminder.sh`。
>
> **漏更会怎样（失败模式是响亮的，但仍须有人认领）**：静态数据只覆盖到当年 12-31 → 跨年后 L2 收到「区间外」请求 → **throw**（**设计如此，禁返空**）→ L1 若同时失效则全链失败 → 日历填充失败 → 心跳陈旧 → 每 4h 探针 🔴。**即：漏更不会静默烂掉，但会在最坏的时点（元旦 + 主源已挂）才炸** —— 那时你在放假，且只剩「现场手搓 PDF」一条路。**故：12 月前更完，别赌**（且现在有月级催更提前几个月盯着你）。
>
> **为什么不能自动化 / 不能从 `trading_day` 快照生成**：① 官方只发 PDF、每年格式微调，无稳定接口；② 🚨 快照一生成即开始腐烂 —— 填充只问「过去 30 天」，但静态表必须**覆盖到今天**，从历史快照生成的表到年中就答不了近 30 天窗。L2 必须源自「年初即发布全年」的官方年历。详见 `scripts/checks/gen-static-calendar.ts` 头注释（含 5 条解析规约：`Half Day` = 交易日 / 禁取 `Northbound/Southbound` 行 等，盲写解析器必踩）。

## 故障排查（本地每日同步）

> holdings / marketdata 两个每日同步的失败模式 + 修复。感知面分工见上文「跑完上报」与「no-show 看门狗」两段（本节不复述）。

### holdings 同步

先分清失败在哪一步：run 流程 = 启 CDP Chrome（固定 profile）→ 同花顺导出 xlsx 下载 → 上传 `api.shintongtech.com`。看 stdout / `~/.nvy/holdings-sync/sync.log` 尾行定位。

1. **`refresh token 已失效（401）`—— 上传步鉴权失效（最常见）**
   - **根因**：holdings 用 003 refresh token（存 `~/.nvy/holdings-sync.json` 的 prod 槽，`upload-holdings.ts` **只在轮转成功时才回写存储**），DB 里是 `device_type=UNKNOWN` 的家族。任何在 App/网页做的**「全端登出 / 退出所有设备」** = server `revokeAllForAccount`（`updateMany where {accountId, revokedAt:null}`）会**连带撤掉 holdings 这条 token 家族**。此后每天 09:00 headless：读死 token → rotate → 401 → **headless 无 TTY 不能短信重登（设计如此）** → 存储不更新 → 次日继续拿同一死 token。
   - **症状识别**：每天同一个 401、`holdings-sync.json` mtime 冻结在最后成功日；**同花顺抓取照常成功（xlsx 正常下载），失败纯在「上传前换 token」这步——别去查抓取 / 62→77 迁移**。refresh TTL = 30d 滑动窗口、轮转重签（`security/refresh-token.rules.ts` `REFRESH_TTL_DAYS`），**不会一天内自然过期** → 401 必是「被撤销」或「账号非 ACTIVE」，不是到期。
   - **修复（只能手动交互重登，短信 2FA 只本人手机能收，无法自动化）**：仓库根 `pnpm holdings:upload --base-url https://api.shintongtech.com`（默认 base-url 是 dev，**必须显式 prod**）→ 输手机号 + 验证码 → 重新种 token 家族写回同一存储 → 定时任务自愈。重登后可 `pnpm holdings:upload --headless --base-url https://api.shintongtech.com` 复现 09:00 上传路径验证（绿 = 好）。
   - **取证（prod DB 只读，走 marketdata 同款只读通路）**：`ssh mbw-staging` → `docker exec -i nvy-tight-postgres-1 psql -U mbw -d mbw`。`refresh_token` / `account` 表在 **`account` schema**（非 security）；token 存纯 **SHA-256 hex** hash（无 salt），拿存储里的 raw token 算 `sha256` 反查行看 `revoked_at`。**「全端登出」的 DB 签名 = 多条 token 精确到毫秒同一 `revoked_at`**（一把 `updateMany` 撤）；账号状态查 `account.account.status`（非 ACTIVE 也折 401）。

2. **`Browser context management is not supported`（connectOverCDP）—— 调试 Chrome 脏状态**：固定 profile 的调试 Chrome（端口 18800，独立 profile）实例脏 → `kill` 18800 监听 PID，下次 sync 自启干净实例修复。

3. **`net::ERR_INTERNET_DISCONNECTED`**：抓取时断网，瞬态，次日自动重试。

### marketdata 同步

- **`本地 dev PG 未就绪` / 连不上 5433 —— 共享 dev PG 被 teardown（#412 修）**：`mbw-poc-postgres` 被交互开发与 09:05 同步**共享**。收工 `docker compose down` **移除**容器（非 stop）→ 次晨原 `docker start` 救不回。修复已落 `sync.sh` §0 改 `docker compose up -d` 重建（setup 把 `docker-compose.dev.yml` 一并拷 `~/.nvy` 保自包含，顶层 `name: mbw-poc` 固定项目名，找不到 compose 兜底退回 `docker start`）；收工纪律见 [`local-dev.md`](./local-dev.md) Teardown（已拆「停 dev」vs「停共享 DB」）。⚠️ 重跑 `setup.sh` 保持默认 `--time`（`setup.sh` 默认即 `09:05`，须晚于 holdings 09:00；两者是独立 launchd label，重跑本任务动不到 holdings 的点——**若显式传别设成 ≤ 09:00**）。
- **dev 缺某股数据（K线 / 指标 / 复权空）= 数据形态所限，非 bug**：同步只灌 instrument 全量（**~2.8 万**：us ~1.95 万 / cn ~5.7 千 / hk ~2.8 千）+ daily_bar「全股最近 20 个交易日 + ~15 支样本股全历史（~729 天）」+ 复权 / 财务 / 基本面 / 公司行动 4 表**仅样本股**。⇒ **样本股全功能**（指标 / 复权 / K线 / EOD 预警需 ≥520 根），**非样本股仅近端价**（quote / 搜索 / 列表）。样本 codes 见 `scripts/jobs/marketdata-dev-sync/sync.sh` 顶部 `SAMPLE_CODES`。每次全量截断重灌（单事务，失败回滚）；prod daily_bar 全 `adjust='none'`（复权读时算，per 020）。
- **`⚠️ 副本落后于仓内 sync.sh` / `⚠️ 仓内 sync.sh 与部署副本不一致` / `⚠️ 副本已 N 天没重新部署` —— 部署漂移，修法一律是 `bash scripts/jobs/marketdata-dev-sync/setup.sh --time 09:05`**：定时任务跑的是 `~/.nvy` 的**冻结副本**，改仓内源不重跑 setup ⇒ 跑的仍是旧逻辑，而**失败形态是「静默的成功」**（日志照打 `✅ 同步完成`、退出码 0、逐表对数全绿——因为对数比的是「旧版声明要同步的那些表」；2026-08-11 实撞：08-10 翻 full 的三张美股期权表少同步了两天没人发现）。自检**只告警不中断**（旧逻辑通常还能跑出部分数据，硬失败等于本地一张表都没有）。⚠️ launchd 那一臂**读不到**仓内源（对 `~/Documents` 无 TCC），只能退化成「部署天龄」这个**代理量**（阈值 `DEPLOY_STALE_DAYS`，默认 14 天）+ 每轮汇总里的 `副本 commit=… 部署于 N 天前`；**正面命中靠仓内手动跑那一臂**（改完脚本手动验一次即当场喊）。印记文件 `~/.nvy/marketdata-dev-sync/deployed.meta` 由 setup 写。
- **`⚠️ 表 X 单表 N 行，已过阈值` —— 趋势预警，不是错误**：单表导出行数超 `ROW_WARN_THRESHOLD`（默认 30 万）就喊一声，数据本身已逐表对数通过。本工具是「截断 → 重灌」全量语义，唯一无上限增长的表是 `marketdata.option_daily_snapshot`（实测约 580 行/标的/交易日，随锚数线性涨），它已按 `session_date` 收窄到近 `OPTION_RECENT_DAYS`（默认 30 自然日 ≈ 21 交易日）。触发时的处置顺序：① 调小 `OPTION_RECENT_DAYS`（消费端功能下界只要 2 个 session）→ ② 给该表换增量语义。
- **同步「成功」但灌进来的是 mock 数据 —— 拿 dev 做统计 / 标定前必须先跑这一眼**：2026-08-12 那期实测 3617 行里 **12 只票共用同一个 `underlying_spot` `128.40`**、整条链每个行权价共用同一组 `bid` / `ask` / `open_interest` / `volume` / `delta`（3617 行只有 **2** 个不同报价元组；健康的 08-11 是 7063 行 / 12 个 spot / 7025 个元组）。日志照打成功、退出码 0、逐表对数全绿 —— **因为对数比的是行数，而行数是有的**。⚠️ 茅台 `1700` 那个 mock 判定锚**认不出这一类**（它只覆盖 A 股 quote），期权链侧要另跑数据形状自检：

  ```sql
  select session_date, count(*) rows,
         count(distinct underlying_spot) spots,                  -- 健康 = 标的数; mock = 1
         count(distinct (bid,ask,open_interest,volume)) quotes   -- 健康 ≈ rows; mock = 个位数
  from marketdata.option_daily_snapshot group by 1 order by 1 desc limit 5;
  ```

  🚨 **为什么它值得独立一条**：下游最常见的取数方式是「每票取自己最近一期」，那会让**全部标的**一起落进被污染的那期；而基于它算出来的阈值 / 分布 / 标定值**全都算得出数**，写进文档时与真标定长得一模一样，此后没有任何一处会红（2026-08-13 `052` T016 实撞，标定改用 `08-11` 那期）。⇒ 统计 / 标定类任务的第一步恒为**验数据面**，不是跑分布。根因从 `MARKETDATA_PROVIDER` 运行态与当轮同步日志入手。

  📌 **上面这条自检的适用范围有水位线（`054` 起）**：`054` 之后本地进程**已不可能**再写入伪造行情 —— `MARKETDATA_PROVIDER=mock` 下 28 个采集口绑的是拒绝壳，一调即抛（见 [ADR-0047](../../docs/adr/0047-marketdata-pluggable-data-access.md) § 2 的 2026-08-13 Amendment）。故：
  - **水位线之前**写入 dev 库的行：**一律判「来源不可考」**——行本身不带来源标识，事后无法分辨哪些是本地进程灌的。不做数据迁移、不加 schema 列，也**不需要**回删：dev 库是「截断 → 重灌」全量语义，本地写入的行在**下一轮成功同步后即归零**，这个集合是封闭且自动递减到空的。
  - **水位线之后**：上面那段形状自检退化为「只需查水位线之前的行」——更强而非更弱。它**仍然保留**，因为它挡的不只是本地写入这一条路（prod 侧误配、vendor 返回退化数据同样会被它抓到）。

- **dev 日志里每天出现的 `MockCollectionRefusedError`「采集被拒」= 预期行为，不是故障（`054` 起）**：本地 server 跑在 `MARKETDATA_PROVIDER=mock` 下时，夜间同步 tick / 两级补救 cron / 日历填充照常触发，撞到采集口即抛专属错误并被写手既有的 `try/catch` 落成日志。**这是刻意的可见信号**——「你的本地进程正在试图采集」，而 2026-08-12 那次事故当天最缺的正是这份可见性（写手一路成功，没有任何一处出声）。⇒ 看到它**不必处置**；真要跑真采集就显式设 `MARKETDATA_PROVIDER=live`。⚠️ 反过来，`kind=mock` 下**看不到**这类日志才值得看一眼：要么定时链根本没跑，要么有人把采集口绑回了 mock。

- **`检测到未注册的新表 marketdata.<t>` —— 新 marketdata 表落 prod 后 fail-loud（设计如此，非 bug）**：sync.sh §1.5 拿 prod `information_schema` 全表对 `TABLE_POLICIES` 做差集，出现未注册表即硬失败（禁无声截断，逼一次显式决策）。⚠️ **报的表名随 PG 返回序变**（`GROUP BY` 无 `ORDER BY`），同批多张新表会逐次命中不同表——**别只补报的那一张**。修：`comm -23 <prod marketdata 表清单> <TABLE_POLICIES 已注册>` 拿**全量**差集，逐行加策略——**港股专属事实表**（`marketScope={hk}`）在 `SAMPLE_CODES` 全 A 股时 `sample_only` 也导 0 行 → 默认 `skip`（注释写清；将来本地联调港股再往 `SAMPLE_CODES` 加港股样本股 + 改 `sample_only` 点亮）；市场级 / 运维表（无 `instrument_id`）只能 `full` / `skip`。改完 `bash scripts/jobs/marketdata-dev-sync/setup.sh --time 09:05` 刷 `~/.nvy` 副本 → `launchctl kickstart -k gui/$(id -u)/com.nvy.marketdata-dev-sync` 验证绿。（2026-07 一次性补 039-043 港股 16 张 + 044 `calendar_sync_health` 共 17 张，全 `skip`）

### 共同前置

- **两任务均撞 macOS TCC**：**launchd agent 对 `~/Documents` 无访问权限**，故任何 launchd 任务都不得 exec `~/Documents/projects/...` 下的脚本（会被系统静默拒绝）。两条解法并存：① **marketdata / watchdog / nvy-private-backup** —— setup 生成器把运行时全部 `cp` 到 `~/.nvy/<task>/` + 共享 lib 到 `~/.nvy/lib/`，plist 只 exec `~/.nvy` 下的 `run-scheduled.sh`（副作用：改仓内源码后**必须重跑对应 setup 覆盖副本**）；② **holdings** —— 因需拉起 GUI Chrome + fnm node，靠用户手动授予 `/bin/zsh` 与 fnm node **完全磁盘访问**。原始锚点见 `scripts/jobs/marketdata-dev-sync/setup.sh` 与 `scripts/jobs/nvy-watchdog/setup.sh` 的头部注释。（2026-07-30 修：本条原写「见 local-dev.md」，但该文件全文无 TCC 相关内容，是坏引用）
- **数据时效**：prod 理杏仁同步 22:00 起、当晚 ~22:30 就位 → 晨间任意时间数据侧都安全；唯一约束 = Mac 须醒（setup 打印 `pmset wakeorpoweron`）。holdings 口径 = 前一交易日收盘（当日成交次日反映）。

## 新增定时任务时（checklist）

1. 写脚本 + 调度单元（systemd `.timer`+`.service`，或本地 launchd 生成器 `scripts/<name>/setup.*`）。脚本只管「干活 + 打 stdout 汇总 + exit 0/非0」，**不要**自己写飞书发送。
2. **飞书上报复用既有基建**（别每加一个调度就重写 webhook/token）：
   - **每日批任务** → 入口套 `ops/lib/nvy-run-reported.sh <task> -- <cmd>`（成功 + 失败都推 report + 写心跳）；并在对应机器看门狗清单加一行（本地 `scripts/jobs/nvy-watchdog/setup.sh`，77 `nvy-watchdog.service`）。⚠️ **本地必须改 `setup.sh` 里的 `TASKS` 默认值，不能只靠调用时传 `--tasks`** —— 否则将来任何人裸跑一次 setup 就会静默摘掉该任务的兜底（2026-07-30 futu-eod 踩过）。
   - **高频监控**（≤ 分钟级）→ **不套** wrapper（会刷屏）；保留自身告警逻辑，飞书发送换成 `. feishu-send.sh; feishu_send "$msg"`；正向 liveness 走每日摘要。
   - webhook/secret 一律复用**本机共享文件**（路径与变量名见上文 host `app` 段末的飞书那条，此处不复述）；systemd 单元加 `EnvironmentFile=-/etc/nvy-alert.env`。**不新建** webhook/token。
3. **本表登记**：归到对应 host section，补一行（任务 / 调度 / 触发器 / 执行 / 仓库锚点 / 用途）。
4. 🚨 **改动怎么到机器上**（`.timer`/`.service`/脚本/谓词改完不会自己生效——**仓里改了 ≠ 机器上跑的那份变了**）：
   - **77（systemd）** → 由 `deploy.yml` 最后一步无条件 `sudo -n bash ops/jobs/install.sh` 自动铺（见 [`prod-deploy-rollback.md` § 部署流程 步 7](prod-deploy-rollback.md#部署流程deployymlssh-进-ecs-跑)）。⚠️ 它由**发版**驱动 —— 改了 ops 但近期不发版就还没生效，急用手动跑同一条命令。
   - **本地（launchd）** → 跑对应 `scripts/<name>/setup.*` 覆盖 `~/.nvy` 下的冻结副本（见上文「自包含副本」注 + 「故障排查」段的部署漂移条）。
   - 这类漏装的失败形态是**静默的**（旧逻辑照跑、退出码 0、告警看不出自己是旧判据），2026-08-17 期权探针假红即此形（#73 合了但没装）。
5. 退役任务：删行 + 在相关 runbook 注明退役（别留 stale 行）。
6. path rule `.claude/rules/scheduled-tasks-registry.md` 会在触及 `.timer` / `scripts/**/setup.*` 时自动加载提醒——但本地 launchd `.plist`（装在 `~/Library/LaunchAgents/`）/ crontab 在仓库外，rule 触发不到，需**手动**记得登记。

## 不在范围（不登记）

- **server 内部 `@nestjs/schedule @Cron`**（`apps/server/`）—— 应用进程内调度，归 server 代码管，非本表（host/OS 级）范围。
- **OS 自带 systemd timer**（`apt-daily{,-upgrade}` / `logrotate` / `sysstat-{collect,summary}` / `fstrim` / `man-db` / `motd-news` / `dpkg-db-backup` / `e2scrub_all` / `fwupd-refresh` / `systemd-tmpfiles-clean` / `update-notifier-*` / `certbot` 的发行版默认部分 …）—— 系统维护，非业务，不登记。
