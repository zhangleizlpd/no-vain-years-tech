/**
 * Generated by orval v8.11.0 🍺
 * Do not edit manually.
 * no-vain-years API
 * no-vain-years backend HTTP API. Generated from NestJS controllers; consumed by packages/api-client for cross-app TS types.
 * OpenAPI spec version: 1.0
 */
import type { OptionsdeskControllerLegsPerspective } from './optionsdeskControllerLegsPerspective';

export type OptionsdeskControllerLegsParams = {
/**
 * 本次覆盖作用于哪个视角。**给了任一条件就 MUST 给它**; 省略 = 无覆盖 (三视角全走默认值)
 */
perspective?: OptionsdeskControllerLegsPerspective;
/**
 * 行权价上界 (闭区间); 空串 = 覆盖为不限
 */
strikeMax?: string;
/**
 * 行权价下界 (闭区间); 空串 = 覆盖为不限
 */
strikeMin?: string;
/**
 * DTE 段下界 (闭区间)。🚨 **与 dteMax MUST 成对出现** —— DTE 段是**一个**维度、值是闭区间, 半个区间不是合法维度值; 静默补另一端要么意外放宽 (补不限), 要么要在这里重算默认值 —— 而那需要 spot, 正是 FR-011 禁的第二处计算。只给一端 → 400
 */
dteMin?: string;
/**
 * DTE 段上界 (闭区间)。与 dteMin 成对, 见其说明
 */
dteMax?: string;
/**
 * 权利金下限; 空串 = 覆盖为不限
 */
premiumMin?: string;
/**
 * 未平仓 (OI) 下限 (张)。🚨 **与 volMin MUST 成对出现** —— 活性是**一个**维度、值是一对数 (`OI ≥ oiMin` **或** `当日成交 ≥ volMin`)，半对不是合法维度值。只给一端 → 400
 */
oiMin?: string;
/**
 * 当日成交 (Vol) 下限 (张)。与 oiMin 成对，见其说明
 */
volMin?: string;
/**
 * 相对价差上界; 空串 = 覆盖为不限。全腿视角的系统默认值本就是不限 (FR-010)
 */
relativeSpreadMax?: string;
};
