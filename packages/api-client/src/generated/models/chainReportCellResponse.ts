/**
 * Generated by orval v8.11.0 🍺
 * Do not edit manually.
 * no-vain-years API
 * no-vain-years backend HTTP API. Generated from NestJS controllers; consumed by packages/api-client for cross-app TS types.
 * OpenAPI spec version: 1.0
 */
import type { ChainReportCellResponseState } from './chainReportCellResponseState';

export interface ChainReportCellResponse {
  /** 格态。valued 有值 / gated **有腿但被门槛挡下** / absent 该位置无合约。🚨 后两者 MUST 视觉可分且**不依赖图例** (FR-017): 「有腿但太便宜」与「压根没这张合约」是两条完全不同的处置路径。📌 `gated` 归并了三类成因 (权利金门槛 / 当前格值视角不召回 / 该口径算不出值), 段内不再细分 —— 那属于选约表那一层 (FR-016a 显式接受的代价) */
  state: ChainReportCellResponseState;
  /** 格内腿数 (FR-007) —— **当前格值下算得出值的成员条数**, 非 valued 恒 0。🚨 它与格态同为**当前格值的函数**, 🚫 MUST NOT 缓存成格的静态属性 (实测全网格填充率 建仓 6.3% / 收租 13.6% / 全腿 41.6%) */
  legCount: number;
  /** 该格**最优**值 (FR-006: 取最优不取均值); 非 valued 恒 null。🚨 **量纲随格值变**, 见所属网格的说明: 建仓成色是百分数 / 两种年化是小数比例 / 活跃度是张数。定标一律 6 位, 客户端按格值决定怎么显示 */
  best: string | null;
  /** 该格**次优**值 (FR-027 读数面板要)。🚨 **格内只有一条腿时显式 null** (FR-028), 🚫 MUST NOT 复述最优值充数 —— 次优存在的意义正是回答「这一格是一条腿撑起来的、还是一片腿都不错」。📌 两条腿取值**相等**时它 = 那个值而**不是** null: 判据是腿数不是取值互异 */
  runnerUp: string | null;
}
