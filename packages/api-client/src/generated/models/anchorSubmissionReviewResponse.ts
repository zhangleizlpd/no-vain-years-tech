/**
 * Generated by orval v8.11.0 🍺
 * Do not edit manually.
 * no-vain-years API
 * no-vain-years backend HTTP API. Generated from NestJS controllers; consumed by packages/api-client for cross-app TS types.
 * OpenAPI spec version: 1.0
 */
import type { AnchorSubmissionReviewResponseAsofFlag } from './anchorSubmissionReviewResponseAsofFlag';
import type { AnchorSubmissionReviewResponseDisposition } from './anchorSubmissionReviewResponseDisposition';
import type { AnchorSubmissionReviewResponseMarket } from './anchorSubmissionReviewResponseMarket';
import type { AnchorSubmissionReviewResponseStatus } from './anchorSubmissionReviewResponseStatus';

export interface AnchorSubmissionReviewResponse {
  /** 待审条目 id (数字串) */
  id: string;
  /** 提交方 (归属, 不作授权) */
  submitter: string;
  /** canonical `market:code` */
  ticker: string;
  /** 标的名。未注册 / ticker 不可解析 ⇒ null,呈现层退回代号 —— MUST NOT 拿 ticker 拼假名字 (那会让「名字没同步上」与「这票就叫这个」在屏上分不开)。 */
  instrumentName: string | null;
  /** 市场段 */
  market: AnchorSubmissionReviewResponseMarket;
  /** 每股内在价值 (字符串, 永不经 JS number) */
  v: string;
  /** 估值口径日 `YYYY-MM-DD` */
  asof: string;
  /** 估值方法名 */
  method: string;
  /** 10 分制置信度 (字符串) */
  confidence: string;
  /** 提交方的估值理由 (只读) —— 决定采不采纳的唯一定性输入 */
  note: string | null;
  /** 审阅方的处置附言 (与上面 note 是两个人写的两件事, 不合并) */
  reviewNote: string | null;
  status: AnchorSubmissionReviewResponseStatus;
  /** 采纳时落成的锚 id;未采纳 ⇒ null */
  consumedAnchorId: string | null;
  /** 采纳这条会新建锚还是刷新既有锚。🚨 refresh **不是更温和的 create**:它会冲掉三处人工位 并把 confidence_source 翻成 model(此后置信度在 App 里改不动)。 */
  disposition: AnchorSubmissionReviewResponseDisposition;
  /** asof 可信度五档。FUTURE=那天收盘价还不存在;NON_TRADING=日历说不开市(含节假日);UNKNOWN=日历没填到那一段(**不等于没问题**);TODAY=就是今天(仅警告, 服务端判不了收没收盘);OK。 */
  asofFlag: AnchorSubmissionReviewResponseAsofFlag;
  /** 需要改期时的建议日 (该市场早于 asof 的最近交易日);日历解不出 ⇒ null,**不猜** */
  asofSuggested: string | null;
  /** 该档是否必须显式确认才放行采纳 */
  asofNeedsAck: boolean;
  /** 收件时刻 (ISO-8601) */
  createdAt: string;
  /** 最后处置/更新时刻 (ISO-8601) */
  updatedAt: string;
}
