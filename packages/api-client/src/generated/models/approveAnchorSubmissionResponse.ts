/**
 * Generated by orval v8.11.0 🍺
 * Do not edit manually.
 * no-vain-years API
 * no-vain-years backend HTTP API. Generated from NestJS controllers; consumed by packages/api-client for cross-app TS types.
 * OpenAPI spec version: 1.0
 */
import type { AnchorFallbackEntryResponse } from './anchorFallbackEntryResponse';
import type { ApproveAnchorSubmissionResponseAction } from './approveAnchorSubmissionResponseAction';
import type { ApproveAnchorSubmissionResponseAsofFlag } from './approveAnchorSubmissionResponseAsofFlag';

export interface ApproveAnchorSubmissionResponse {
  /** create=新建了锚(⇒ 当日采集要为它多做一整轮历史回填);update=刷新了既有锚;noop=值全等, 什么都没写 */
  action: ApproveAnchorSubmissionResponseAction;
  /** 落成/命中的锚 id */
  anchorId: string;
  /** canonical `market:code` */
  ticker: string;
  /** 真正落库的口径日 —— 与提交行不同即说明被改过 (审核方改的或 shift 改的) */
  appliedAsof: string;
  asofFlag: ApproveAnchorSubmissionResponseAsofFlag;
  /** 本次真正冲掉的人工位, 逐条列出。**禁静默回落** —— 为空是正常的。 */
  fallbackEntries: AnchorFallbackEntryResponse[];
  /** 收件箱状态是否成功翻成 CONSUMED。**false 不是失败** —— 锚已经写了, 只是并发处置抢跑 导致状态没翻。此时 MUST 提示人工核对, 且 MUST NOT 重试(会写第二遍)。 */
  statusFlipped: boolean;
  /** action=create ⇒ 会排一个冷启动 job (分钟级, worker concurrency=1 串行) */
  coldStartExpected: boolean;
}
