/**
 * Generated by orval v8.11.0 🍺
 * Do not edit manually.
 * no-vain-years API
 * no-vain-years backend HTTP API. Generated from NestJS controllers; consumed by packages/api-client for cross-app TS types.
 * OpenAPI spec version: 1.0
 */
import type { ChainReportBandCoverageResponse } from './chainReportBandCoverageResponse';

export interface ChainReportColumnResponse {
  /** 到期日 (YYYY-MM-DD) */
  expiryDate: string;
  /** 期限天数 (整数日历日, 到期日当天 = 0) */
  dteDays: number;
  /** 是否月度到期链 —— 判据与选约表**同一处** (该月第三个周五, 非交易日则取其前一交易日)。📌 **「是否跨财报」蓄意不在本响应内** (2026-08-14 定): mockup 未画、零 FR 要求, 且列头多一个 chip 要吃掉 FR-041 已经很紧的一屏高度预算 */
  isMonthlyChain: boolean;
  /** 该到期日的**平值**隐含波动率, vendor 原样**百分数** (25.5 = 25.5%), 由跨现价两侧的相邻行权价线性插值得出 (FR-022)。🚨 **插值不可得 ⇒ null ⇒ 曲线该点断开** (FR-023), 🚫 MUST NOT 以任何形式填充 —— 补一个值进去, 用户读到的就是一条连续的期限结构, 而它有一段是编的。🚫 客户端 MUST NOT 再 ×100 */
  atmIv: number | null;
  /** **每种格值**下本列是否落在其对应视角的召回段内。一个字段同时服务两处呈现, 蓄意不拆: FR-009 的两条召回段范围框取前两项 (重叠列两框并存, 🚫 不归给其中一段); FR-009a 的整列淡出看**当前格值**那一项。🚨 客户端 MUST NOT 自己做「格值 → 视角」的映射 —— 两处各写一份会出现「格有值但整列淡出」这种自相矛盾, 而两边都渲染得出来 */
  inRecallBand: ChainReportBandCoverageResponse;
}
