# 2026-05-24 工作日志

## 主线（本 session）：客户端部署 — 子 plan 1 CF Web 上线 ✅

延续昨日 server prod cutover（#144/#145/#147，`api.xiaocaishen.me` 上线），启动客户端部署。用主/子 plan 体例起 [master plan](../plans/2026-05/05-24-client-deploy-web-android-ios-master.md)（统领 4 子 plan：CF Web / Mobile 地基 / Android / iOS）。构建链路决策（用户拍板）：**EAS 云构建唯一**（国内慢优先挂代理，`--local` 次选），Android **签名 APK 直分发**，iOS **两阶段**（simulator → 账号就绪后 TestFlight）。

### 子 plan 1（CF Web）落地 + 一次弯路

| # | 内容 |
|---|---|
| #169 | master + p1 plan + `apps/mobile/public/_redirects`（SPA fallback）|
| #171 | 修 prod 重启命令漏 `--env-file .env.production`（SWAS 实操 ACR pull denied 撞到）|
| #172→#173 | ⚠️ **弯路**：跟 CF 2026-02「start with Workers」官方建议 pivot 到 Workers Static Assets → 加自定义域撞 "No zones found" → Workers 自定义域要求域名是本账号 CF zone，而 `xiaocaishen.me` NS 在阿里云万网（外部 DNS）→ **当日 revert 回 Pages**（Pages 支持外部 DNS 子域 CNAME）|
| #175 | 修 bug：`setup.ts` 用方括号 `process.env['EXPO_PUBLIC_…']`，Expo 只 inline **点号** → 任何 web 部署都静默打 localhost（grep 实测验证修复）|
| 部署 | CF Pages 项目 `no-vain-years-mono`（`wrangler pages deploy` direct-upload）+ 自定义域 `app.xiaocaishen.me`（阿里云 DNS CNAME）|
| 收尾 | 删旧 meta Pages 项目 `no-vain-years-app`（先清 ~24 deployment）；状态翻 ✅（#177）|

**验证**：`https://app.xiaocaishen.me` 首页/深链 200（SPA 回退）+ CORS preflight allow-origin + 线上 bundle 命中 prod API（非 localhost），端到端通。

### 沉淀（memory）

- `feedback_china_network_prefer_domestic_proxy` — 国内访问境外云服务慢，第一选择挂可靠国内代理保住链路
- `reference_cf_workers_custom_domain_requires_cf_zone` — 选 CF 静态托管形态前先 `dig NS`；外部 DNS 域名只能 Pages 不能 Workers
- `feedback_design_tokens_reuse_not_redesign`（边界纠偏）— 禁 claude-design 仅针对已稳定 token，不限制缺失启动图标生成；图标优先复用 meta app 仓

### 复盘

- **瓶颈**：Workers pivot 绕一圈（#172→#173）。根因 = 跟官方建议 pivot 前**没先查 DNS 托管在哪**。
- **优化**：选外部产品形态前先验关键约束（DNS / 凭据 / 网络），别凭官方"推荐"直接走。

## 其他主线（并行，headline — 非本 session 细节）

- **扁平贫血范式重构 R-1~R-6**（#157–#168）：拍平 hexagonal 模块目录、贫血化 Account 直注 Prisma（删 repository port）、VO 拍平为纯函数、auth 两段式委托护城河、ts-morph 跨 ctx 探针；ADR-0043 Accepted、ADR-0020 Superseded、constitution v1.1.0。
- **ADR realign P1**（#148–#156, PR-1~8）：清 ADR-0001..0017 悬空 meta 引用、status 校正、叙事重写、ADR index 机械校验。
- **meta 历史债清理**（#170/#174/#176）：governance 脚本归入 `scripts/checks/` nx 项目、清 ADR/workflow/注释里的 meta-server 叙事。

## 状态 / 下一步

- ✅ 子 plan 1（CF Web）DONE — `app.xiaocaishen.me` 上线。
- ⏭️ 子 plan 2（Mobile 地基）待起：独立 `/plan`，起手 `! eas login` → assets/eas.json/eas init/version 对齐 + ADR-0044 → 解锁 Android（子 plan 3）/ iOS（子 plan 4）。
